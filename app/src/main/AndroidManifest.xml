<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="projects.chirolhill.juliette.finalprojectchirolhilljuliette">
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@drawable/blueelephant"
        android:label="@string/app_name"
        android:roundIcon="@drawable/greenelephantcircle"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".MainActivity">

        </activity>
        <activity android:name=".SignInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AskTokiActivity">

        </activity>
        <activity android:name=".MissionActivity">
        </activity>
        <activity android:name=".ProgressActivity">
        </activity>
    </application>

</manifest>